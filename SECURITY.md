# 安全策略

## 报告安全问题

请勿在公开 issue 中报告安全漏洞。请通过邮件私下联系维护者(见仓库页面)。

所有安全报告将在 72 小时内确认,并在修复或缓解后尽快公开披露。

## 安全范围

本项目在 dsh 宿主进程内运行,并直接调用本机的 AGY CLI。以下与安全相关:

- **密钥与凭据**:AGY 的登录凭据由本机系统凭据管理;插件本身不存储任何密钥
- **代理**:`proxy` 配置会作为环境变量注入 AGY 子进程;请勿在共享环境中配置不受信任的代理
- **本地执行**:插件会 spawn 本机 `agy` 命令;请确保 `command` 配置指向可信的可执行文件
- **不修改 dsh 源码**:插件仅通过官方扩展点装配,不引入供应链篡改面

## 受支持的版本

| 版本 | 支持状态 |
| --- | --- |
| 最新版本 | ✅ 积极支持 |
| 更早版本 | ⚠️ 按需修复 |
